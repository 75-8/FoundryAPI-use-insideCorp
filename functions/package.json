{
  "name": "apim-log-pipeline",
  "version": "1.0.0",
  "description": "APIM - Azure AI Foundry 通信ログ収集パイプライン (spec_function.md v2.3)",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest --forceExit --detectOpenHandles"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "@azure/identity": "^4.2.0",
    "@azure/storage-blob": "^12.24.0",
    "@azure/storage-queue": "^12.23.0",
    "@azure/monitor-ingestion": "^1.1.0",
    "@dsnp/parquetjs": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}
